import React, {Fragment} from 'react';
import Header from '../../components/Header';

const CreateTransactions = () => {
    return(
        <Fragment>
            <Header />
            <p>Hi, I am create transactions view</p>
        </Fragment>
    );
}

export default CreateTransactions;